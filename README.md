This repo provides functionality for analyzing networks.

Make sure you have anaconda installed on your machine.

To setup, run `conda env create -f env.yml`
Then run `conda activate network-research-python`